#include<stdio.h>

void main()
{
    int a = 10;
    if(______)
        printf(" %d", a);
    else 
        printf("%d %d", a, a);
}

/*
 * Which of the following checks will print 10 twice?
 * 1). a == 10
 * 2). a <= 10
 * 3). printf("%d", a);
 * 4). None of the above
 *
 * Ans: 3). printf("%d", a); 
 */
