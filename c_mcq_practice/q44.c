/*
 * Difference between structure and union is
 * 1). define functions within structures but not within a union
 * 2). define functions within union but not within a structure
 * 3). the way memory is allocated
 * 4). there is no difference
 *
 * Ans: 3). the way memory is allocated
 */
