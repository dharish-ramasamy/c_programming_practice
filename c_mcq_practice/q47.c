/*
 * Which among the following is never possible in C when members in a structure
 * are same as that in a union?
 *
 *   * Let P be a structure
 *   * Let Q be a union
 *
 * 1). sizeof(P) > sizeof(Q)
 * 2). sizeof(P) = sizeof(Q)
 * 3). sizeof(P) < sizeof(Q)
 * 4). None of the above
 *
 * Ans: 3). sizeof(P) < sizeof(Q)
 */
